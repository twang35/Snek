{
  "project": "snek3",
  "id": "b7ai-fc300x100-seed1",
  "type": "train",
  "policy": "b7ai-fc300x100-seed1",
  "max_steps": 50003968,
  "env": {
    "SNEK_ALGO": "ppo",
    "SNEK_REWARD_PRESET": "b2",
    "SNEK_CHASE_SAFE_SHAPING": "0.1",
    "SNEK_CHASE_SAFE_GATE": "75",
    "SNEK_FOOD_DISTANCE_REWARD": "0",
    "SNEK_FC_LAYERS": "300,100",
    "SNEK_PPO_EPOCHS": "4",
    "SNEK_EVAL_QUEUE": "1",
    "SNEK_EVAL_QUEUE_DEPTH": "16",
    "SNEK_EVAL_WORKERS": "8",
    "SNEK_SEED": "1"
  },
  "priority": 30,
  "label": "b7: fc (300,100), seed 1 of 4 \u2014 wave 2 of 4",
  "notes": "Batch b7, the fc-layout sweep: 8 layouts x 4 seeds at 50M transitions, two layouts per wave so each wave is a self-contained comparison. Every knob but the network is b3's reference, which is also PPO's default -- lr 3e-4, gamma 0.99, lambda 0.98, entropy 0.01, 128x128 rollout, minibatch 256, 4 epochs -- on b2's reward function, seeds 1-4 pinned so the layouts are seed-matched to each other and to b1/b2/b29/b41/b47. Epochs are 4, not 8: at 8 seeds fc (200,100) scored 12.8% of rows >=98%/500 at 4 epochs (b6) and 7.3% at 8 epochs (b4), while fc 320 at 8 epochs scored 9.6% (b5) -- so the two knobs interact negatively and 4 epochs is the setting the best result to date used. 50M is a quarter of b4/b5/b6's horizon, so a comparison against those three truncates. b3 ranked this first on density (9.0% at n=1, 20M). Four seeds is the first real test of that ranking."
}
