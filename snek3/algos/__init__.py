"""The learning algorithms, one package each, behind the seam `algos/dqn/algo.py` documents.

`train.py` drives every one of them through the same fourteen members and registers each in
`train.ALGOS`; a new algorithm is a new package here and one entry there, never a second trainer.
The series of algorithms planned against this game is `plans/algoExploration/`.
"""
